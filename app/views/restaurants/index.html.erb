<h1>Restaurants</h1>

<% @restaurants.each do |res| %>
<ul>
  <li>
    Name: <b><%= link_to res.name, restaurant_path(res.id) %></b> - Adress: <%= res.address %>
  </li>
</ul>

<% end %>

<%= link_to 'Registrer new resturant', new_restaurant_path, class: "btn btn-primary" %>
